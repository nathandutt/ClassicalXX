# -------------------------------
# Simulation Parameters
# -------------------------------
x_range 200
dx 0.2
dt 0.02 #small in front of dx^2              
t_max 300
save_every 1

# -------------------------------
# Initial Condition Parameters
# -------------------------------
theta_l 0.1     # theta_l and r in units of pi
theta_r 0.9
v_l 0.
v_r 0.

# Smoothing
smoothing 10            # epsilon = smoothing * dx

